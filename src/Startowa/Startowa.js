import React, { Component } from 'react';
import PropTypes from 'prop-types';

class Startowa extends Component {
  render() {
    return (
      <div className="startowa-cont">
        START
      </div>
    );
  }
}

Startowa.propTypes = {

};

export default Startowa;